fastapi
uvicorn[standard]
sqlalchemy
psycopg[binary]
alembic
pydantic-settings
python-multipart


pyjwt
pwdlib[argon2]
email-validator

openai-whisper
transformers
torch
peft

faster-whisper
ctranslate2
silero-vad
onnxruntime
websockets
rapidfuzz
soundfile

# English COMMAND-mode audio path (app.streaming.audio_command_classifier,
# app.streaming.wake_detector): openWakeWord's EAR embeddings feeding a
# scikit-learn pipeline fitted by openWake/train_classifier.py. Pinned - the
# .joblib model files are unpickled with this exact scikit-learn version;
# an unpinned upgrade can silently change prediction behaviour or refuse to
# load the pickle at all.
librosa
openwakeword
scikit-learn==1.9.0
joblib

pytest
pytest-asyncio
